version: 2

models:
  - name: base_factorial_job_postings
    description: |
      Bronze layer model for job postings from Athena.
      Raw data ingestion with minimal transformation.
      
      **Source**: Athena
      **Grain**: [TODO: Define grain]
    columns:
      - name: id
        description: "[TODO: Add description]"

  - name: base_factorial_candidates
    description: |
      Bronze layer model for candidates from Athena.
      Raw candidate data with minimal processing.
      
      **Source**: Athena

      **Grain**: [TODO: Define grain]
    columns:
      - name: id
        description: "[TODO: Add description]"

  - name: base_factorial_applications
    description: |
      Bronze layer model for applications from Athena.
      Raw application data.
      
      **Source**: Athena
      
      **Grain**: [TODO: Define grain]
    columns:
      - name: id
        description: "[TODO: Add description]"

  - name: base_factorial_hiring_stages
    description: |
      Bronze layer model for hiring stages from Athena.
      
      **Source**: Athena
      **Grain**: [TODO: Define grain]
    columns:
      - name: id
        description: "[TODO: Add description]"

  - name: base_factorial_sources
    description: |
      Bronze layer model for application sources from Athena.
      
      **Source**: Athena
      **Grain**: [TODO: Define grain]
    columns:
      - name: id
        description: "[TODO: Add description]"

  - name: base_file_hiring_process
    description: |
      Bronze layer model for hiring process data from file.
      
      **Source**: File upload
      **Grain**: [TODO: Define grain]
    columns:
      - name: id
        description: "[TODO: Add description]"

  - name: base_factorial_application_phases
    description: |
      Bronze layer model for application phases from Athena.
      
      **Source**: Athena
      **Grain**: [TODO: Define grain]
    columns:
      - name: id
        description: "[TODO: Add description]"

  - name: base_factorial_offer_letters
    description: |
      Bronze layer model for offer letters from Athena.
      
      **Source**: Athena
      **Grain**: [TODO: Define grain]
    columns:
      - name: id
        description: "[TODO: Add description]"

  - name: base_factorial_job_posting_cdc
    description: |
      Bronze layer model for job posting change data capture (CDC) from Athena.
      Tracks changes to job postings over time.
      
      **Source**: Athena CDC
      **Grain**: [TODO: Define grain]
    columns:
      - name: id
        description: "[TODO: Add description]"
